import { DragHelperDirective } from './drag-helper.directive';

describe('DragHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new DragHelperDirective();
    expect(directive).toBeTruthy();
  });
});
