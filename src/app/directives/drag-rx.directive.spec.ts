import { DragRxDirective } from './drag-rx.directive';

describe('DragRxDirective', () => {
  it('should create an instance', () => {
    const directive = new DragRxDirective();
    expect(directive).toBeTruthy();
  });
});
